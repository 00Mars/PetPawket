export type ShareTemplate = {
  id: string;
  title: string;
  description: string;
  format: string;
};

export type StickerPack = {
  id: string;
  title: string;
  description: string;
  tags: string[];
};

export type ShareChallenge = {
  id: string;
  title: string;
  description: string;
};
