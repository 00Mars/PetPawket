export type ProfileSnapshot = {
  pawprints: number;
  circles: number;
  badges: number;
  stories: number;
  memberSince: string;
};

export type ProfileBadge = {
  id: string;
  title: string;
  description: string;
};
